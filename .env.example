# Database Configuration
# Use "sqlite" for local development or "postgres" for production
DB_DRIVER=sqlite
# For SQLite: path to database file
# For PostgreSQL: connection string
DATABASE_URL=sticktoss.db
# Example PostgreSQL URL:
# DATABASE_URL=postgres://user:password@localhost:5432/sticktoss?sslmode=disable

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=dev-secret-change-in-production

# Server Configuration
PORT=8080
GIN_MODE=debug

# For production, use:
# GIN_MODE=release
